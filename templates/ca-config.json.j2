{
  "signing": {
    "default": {
      "expiry": "{{ ca_config_signing_expiry }}"
    },
    "profiles": {
      "kubernetes": {
        "usages": ["signing", "key encipherment", "server auth", "client auth"],
        "expiry": "{{ ca_config_signing_expiry }}"
      }
    }
  }
}